export default defineAppConfig({
  ui: {
    // Map Nuxt UI semantic colors to your custom theme colors
    colors: {
      primary: "brand",
      secondary: "accent",
      neutral: "zinc",
    },
  },
});
